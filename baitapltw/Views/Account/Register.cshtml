@model baitapltw.Models.RegisterViewModel
@{
    ViewBag.Title = "Register";
    Layout = "~/Views/Shared/Signup.cshtml";
}


@using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{

    @Html.AntiForgeryToken()
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="input-form">
        <span>Email-Của Bạn</span>
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
        </div>
    </div>
    <div class="input-form">
        <span>Mật Khẩu</span>
        <div class="col-md-10">
            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
        </div>
    </div>
    <div class="input-form">
        <span>Nhập Lại Mật Khẩu</span>
        <div class="col-md-10">
            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control" })
        </div>
    </div>
    <div class="input-form">
        <input type="submit" class="btn btn-default" value="Register" />
    </div>
    <div class="input-form" style="text-align: center">
        <p>Bạn Đã Có Tài Khoản? <a href="/Account/Login">Đăng Nhập</a></p>
    </div>
}





@*@section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }*@

